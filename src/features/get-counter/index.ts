import counterReducer, { decrement, increment, incrementByAmount, selectCount } from './model/counterSlice';
import { GetCounter } from './ui';

export { increment, incrementByAmount, decrement, selectCount, counterReducer };
export default GetCounter;
